# .github/workflows/pr-rule-validation.yml
name: PR Rule Validation

on:
  pull_request:
    types: [opened, synchronize, reopened, edited]

permissions:
  contents: read
  pull-requests: write

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Get PR information
        id: pr-info
        run: |
          # PR 기본 정보
          echo "pr_number=${{ github.event.pull_request.number }}" >> $GITHUB_OUTPUT
          echo "base_ref=${{ github.event.pull_request.base.ref }}" >> $GITHUB_OUTPUT
          echo "head_ref=${{ github.event.pull_request.head.ref }}" >> $GITHUB_OUTPUT
          
          # 커밋 개수 계산
          COMMIT_COUNT=$(git rev-list --count origin/${{ github.event.pull_request.base.ref }}..origin/${{ github.event.pull_request.head.ref }})
          echo "commit_count=$COMMIT_COUNT" >> $GITHUB_OUTPUT
          
          # 커밋 메시지 수집
          git log origin/${{ github.event.pull_request.base.ref }}..origin/${{ github.event.pull_request.head.ref }} --pretty=format:"%s%n%b" > commit_messages.txt
          
          # 커밋 메시지에서 이슈 키 추출 (PROD-1, CO-2, ABC-123 등)
          COMMIT_ISSUES=$(cat commit_messages.txt | grep -oE '[A-Z]+-[0-9]+' | sort -u | wc -l)
          echo "commit_linked_issues=$COMMIT_ISSUES" >> $GITHUB_OUTPUT
          
          # 추출된 이슈 키 목록 (디버깅용)
          cat commit_messages.txt | grep -oE '[A-Z]+-[0-9]+' | sort -u > commit_issue_keys.txt || touch commit_issue_keys.txt
          
          # 전체 변경 라인 수 계산
          git diff origin/${{ github.event.pull_request.base.ref }}...origin/${{ github.event.pull_request.head.ref }} --shortstat > diff_stat.txt
          TOTAL_CHANGED_LINES=$(awk '{print $4+$6}' diff_stat.txt)
          echo "total_changed_lines=${TOTAL_CHANGED_LINES:-0}" >> $GITHUB_OUTPUT
          
          # 변경된 파일 목록
          git diff --name-only origin/${{ github.event.pull_request.base.ref }}...origin/${{ github.event.pull_request.head.ref }} > changed_files.txt
          
          # 테스트 파일 제외한 변경 라인 수 계산 + 120자 초과 라인 검사
          NON_TEST_LINES=0
          TEST_LINES=0
          LONG_LINES_COUNT=0
          
          # 120자 초과 라인 상세 정보 저장
          > long_lines_detail.txt
          
          while IFS= read -r file; do
            # 테스트 파일인지 확인 (대소문자 구분 없이)
            if echo "$file" | grep -qiE '(test|spec|__tests__|__mocks__)'; then
              # 테스트 파일의 변경 라인 수
              FILE_CHANGES=$(git diff origin/${{ github.event.pull_request.base.ref }}...origin/${{ github.event.pull_request.head.ref }} -- "$file" | grep -E '^[+-]' | grep -vE '^(---|\+\+\+)' | wc -l)
              TEST_LINES=$((TEST_LINES + FILE_CHANGES))
            else
              # 일반 파일의 변경 라인 수
              FILE_CHANGES=$(git diff origin/${{ github.event.pull_request.base.ref }}...origin/${{ github.event.pull_request.head.ref }} -- "$file" | grep -E '^[+-]' | grep -vE '^(---|\+\+\+)' | wc -l)
              NON_TEST_LINES=$((NON_TEST_LINES + FILE_CHANGES))
          
              # 120자 초과 라인 검사 (추가된 라인만, + 기호 제외하고 계산)
              git diff origin/${{ github.event.pull_request.base.ref }}...origin/${{ github.event.pull_request.head.ref }} -U0 -- "$file" | \
                awk '/^\+/ && !/^\+\+\+/ {
                  line = substr($0, 2);  # + 기호 제거
                  if (length(line) > 120) {
                    print FILENAME ":" NR ":" length(line) ":" line;
                    count++;
                  }
                } END {
                  if (count > 0) print "FILE_TOTAL:" count;
                }' FILENAME="$file" >> long_lines_detail.txt
            fi
          done < changed_files.txt
          
          # 120자 초과 라인 총 개수 계산
          LONG_LINES_COUNT=$(grep -c "^.*:.*:.*:.*" long_lines_detail.txt || echo "0")
          
          echo "non_test_changed_lines=$NON_TEST_LINES" >> $GITHUB_OUTPUT
          echo "test_changed_lines=$TEST_LINES" >> $GITHUB_OUTPUT
          echo "long_lines_count=$LONG_LINES_COUNT" >> $GITHUB_OUTPUT
          
          # 테스트 파일 존재 여부
          TEST_FILE_COUNT=$(cat changed_files.txt | grep -iE '(test|spec|__tests__|__mocks__)' | wc -l)
          echo "has_tests=$([[ $TEST_FILE_COUNT -gt 0 ]] && echo 'true' || echo 'false')" >> $GITHUB_OUTPUT
          
          echo "=== PR Statistics ==="
          echo "Total changed lines: $TOTAL_CHANGED_LINES"
          echo "Non-test changed lines: $NON_TEST_LINES"
          echo "Test changed lines: $TEST_LINES"
          echo "Has test files: $([[ $TEST_FILE_COUNT -gt 0 ]] && echo 'true' || echo 'false')"
          echo "Commit linked issues: $COMMIT_ISSUES"
          echo "Long lines (>120 chars): $LONG_LINES_COUNT"
          echo "Found issue keys:"
          cat commit_issue_keys.txt || echo "None"

      - name: Validate PR Rules
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            
            const commitCount = parseInt('${{ steps.pr-info.outputs.commit_count }}');
            const totalChangedLines = parseInt('${{ steps.pr-info.outputs.total_changed_lines }}');
            const nonTestChangedLines = parseInt('${{ steps.pr-info.outputs.non_test_changed_lines }}');
            const testChangedLines = parseInt('${{ steps.pr-info.outputs.test_changed_lines }}');
            const hasTests = '${{ steps.pr-info.outputs.has_tests }}' === 'true';
            const commitLinkedIssues = parseInt('${{ steps.pr-info.outputs.commit_linked_issues }}');
            const longLinesCount = parseInt('${{ steps.pr-info.outputs.long_lines_count }}');
            const pr = context.payload.pull_request;
            const prBody = pr.body || '';
            const prTitle = pr.title || '';
            
            // 120자 초과 라인 상세 정보 읽기
            let longLinesDetails = [];
            try {
              const longLinesContent = fs.readFileSync('long_lines_detail.txt', 'utf8');
              const lines = longLinesContent.split('\n').filter(line => line.includes(':'));
            
              longLinesDetails = lines.map(line => {
                const parts = line.split(':');
                if (parts.length >= 4) {
                  return {
                    file: parts[0],
                    lineNumber: parts[1],
                    length: parseInt(parts[2]),
                    content: parts.slice(3).join(':').substring(0, 100) // 처음 100자만
                  };
                }
                return null;
              }).filter(item => item !== null);
            } catch (error) {
              console.log('No long lines detail file found');
            }
            
            console.log('PR Info:', {
              number: pr.number,
              title: prTitle,
              commitCount,
              totalChangedLines,
              nonTestChangedLines,
              testChangedLines,
              hasTests,
              commitLinkedIssues,
              longLinesCount,
              prBodyLength: prBody.length
            });
            
            // 검증 결과 저장
            const violations = [];
            const checks = [];
            
            // 1. 커밋 개수 검증
            const MAX_COMMITS = 3;
            if (commitCount > MAX_COMMITS) {
              violations.push({
                category: 'Pull Request 크기 규칙',
                message: `**커밋 개수 초과**: 현재 ${commitCount}개 (최대 허용: ${MAX_COMMITS}개)`,
                detail: '커밋을 3개 이하로 squash 해주세요'
              });
            }
            checks.push(`커밋 개수: ${commitCount}개 (최대 ${MAX_COMMITS}개)`);
            
            // 2. 변경 라인 수 검증 (테스트 코드 제외)
            const MAX_NON_TEST_LINES = 200;
            const MAX_TEST_LINES = 500;
            
            if (nonTestChangedLines > MAX_NON_TEST_LINES) {
              violations.push({
                category: 'Pull Request 크기 규칙',
                message: `**변경 라인 수 초과**: 현재 ${nonTestChangedLines}줄 (테스트 코드 제외, 최대 허용: ${MAX_NON_TEST_LINES}줄)`,
                detail: 'PR을 더 작은 단위로 분리해주세요'
              });
            }
            
            // 상세 정보 표시
            if (hasTests) {
              checks.push(`변경 라인 수: ${nonTestChangedLines}줄 (테스트 코드 제외, 최대 ${MAX_NON_TEST_LINES}줄)`);
              checks.push(`테스트 코드: ${testChangedLines}줄 (최대 ${MAX_TEST_LINES}줄, 참고용)`);
            } else {
              checks.push(`변경 라인 수: ${nonTestChangedLines}줄 (테스트 코드 없음, 최대 ${MAX_NON_TEST_LINES}줄)`);
            }
            
            // 2-1. 라인 길이 검증 (120자 초과)
            const MAX_LINE_LENGTH = 120;
            if (longLinesCount > 0) {
              // 상위 5개만 표시
              const topViolations = longLinesDetails.slice(0, 5);
              let detailMessage = `다음 ${topViolations.length}개 라인이 ${MAX_LINE_LENGTH}자를 초과합니다:\n`;
            
              topViolations.forEach((item, index) => {
                detailMessage += `  ${index + 1}. \`${item.file}\` 라인 ${item.lineNumber}: ${item.length}자\n`;
              });
            
              if (longLinesCount > 5) {
                detailMessage += `  ... 외 ${longLinesCount - 5}개 라인\n`;
              }
            
              violations.push({
                category: '코드 스타일 규칙',
                message: `**라인 길이 초과**: ${longLinesCount}개 라인이 ${MAX_LINE_LENGTH}자를 초과합니다`,
                detail: detailMessage.trim()
              });
            }
            checks.push(`라인 길이: ${longLinesCount}개 라인이 ${MAX_LINE_LENGTH}자 초과 (권장: 0개)`);
            
            // 3. 연결된 카드/이슈 개수 검증 (Jira 스타일: PROD-1, CO-2 등)
            // PR Title에서 이슈 키 추출
            const titleIssueMatches = prTitle.match(/[A-Z]+-\d+/g) || [];
            
            // PR Description에서 이슈 키 추출
            const bodyIssueMatches = prBody.match(/[A-Z]+-\d+/g) || [];
            
            // 모든 이슈 키 합치기 (중복 제거)
            const prLinkedIssues = new Set([...titleIssueMatches, ...bodyIssueMatches]);
            const prLinkedCount = prLinkedIssues.size;
            
            // PR Description/Title과 커밋 메시지 모두 체크
            const totalLinkedCount = prLinkedCount > 0 ? prLinkedCount : commitLinkedIssues;
            
            // 발견된 이슈 키 목록
            const foundIssueKeys = Array.from(prLinkedIssues);
            
            console.log('Found issue keys in PR:', foundIssueKeys);
            console.log('Found issue count in commits:', commitLinkedIssues);
            
            const MAX_LINKED = 1;
            const MIN_LINKED = 1;
            
            if (totalLinkedCount > MAX_LINKED) {
              violations.push({
                category: 'Pull Request 크기 규칙',
                message: `**연결된 카드 초과**: 현재 ${totalLinkedCount}개 (최대 허용: ${MAX_LINKED}개)`,
                detail: 'PR을 분리하여 각각 하나의 카드에만 연결해주세요'
              });
            }
            
            if (totalLinkedCount < MIN_LINKED) {
              violations.push({
                category: 'Pull Request 연결 규칙',
                message: `**연결된 카드 없음**: PR Title, Description 또는 커밋 메시지에 카드 번호가 없습니다`,
                detail: 'PR Title에 "[PROD-123]" 형식으로 카드를 연결하거나, PR Description 또는 커밋 메시지에 "PROD-123" 형식으로 카드 번호를 포함해주세요'
              });
            }
            
            // 연결 위치 표시
            let linkedLocation = '';
            let linkedDetails = [];
            
            if (titleIssueMatches.length > 0) {
              linkedDetails.push(`Title: ${titleIssueMatches.join(', ')}`);
            }
            if (bodyIssueMatches.length > 0) {
              linkedDetails.push(`Description: ${bodyIssueMatches.join(', ')}`);
            }
            if (commitLinkedIssues > 0) {
              linkedDetails.push(`커밋 메시지: ${commitLinkedIssues}개`);
            }
            
            if (linkedDetails.length > 0) {
              linkedLocation = ` (${linkedDetails.join(', ')})`;
            }
            
            checks.push(`연결된 카드: ${totalLinkedCount}개${linkedLocation} (최소 ${MIN_LINKED}개, 최대 ${MAX_LINKED}개)`);
            
            // 4. PR Description 필수 섹션 검증
            const requiredSections = [
              { 
                name: '테스트 방법', 
                patterns: [/##?\s*테스트\s*방법/i, /##?\s*test\s*method/i, /##?\s*testing/i] 
              }
            ];
            
            const missingSections = [];
            const foundSections = [];
            
            for (const section of requiredSections) {
              const found = section.patterns.some(pattern => pattern.test(prBody));
              if (found) {
                foundSections.push(section.name);
              } else {
                // 테스트 방법 예외 확인
                if (section.name === '테스트 방법') {
                  // 테스트 코드가 있거나, rename-only refactoring인지 확인
                  const isRenameOnly = /rename|리네임|이름\s*변경/i.test(prBody) && 
                                       !/기능|feature|추가|add|수정|modify|변경|change/i.test(prBody);
            
                  if (!hasTests && !isRenameOnly) {
                    missingSections.push(section.name);
                  } else if (hasTests || isRenameOnly) {
                    foundSections.push(`${section.name} (${hasTests ? '테스트 코드로 대체' : '예외 적용'})`);
                  }
                } else {
                  // 카드 링크로 대체 가능
                  if (totalLinkedCount === 0) {
                    missingSections.push(section.name);
                  } else {
                    foundSections.push(`${section.name} (카드 링크로 대체 가능)`);
                  }
                }
              }
            }
            
            // 누락된 섹션이 있으면 위반 추가
            if (missingSections.length > 0) {
              for (const section of missingSections) {
                violations.push({
                  category: 'Pull Request 설명 규칙',
                  message: `**필수 섹션 누락**: "${section}" 섹션이 없습니다`,
                  detail: section === '테스트 방법' 
                    ? '변경 사항을 어떻게 테스트했는지 설명을 추가하거나 테스트 코드를 포함해주세요'
                    : `"${section}" 섹션을 추가하거나 관련 카드를 링크해주세요`
                });
              }
            }
            
            // 검증 결과 생성
            const timestamp = new Date().toISOString().replace('T', ' ').substring(0, 19) + ' UTC';
            let validationResult = '';
            
            if (violations.length > 0) {
              // 위반 사항이 있는 경우
              validationResult = `<!-- PR_RULE_VALIDATION_START -->
            ## 🤖 PR 규칙 검증 결과
            
            ### ❌ 규칙 위반 (${violations.length}개)
            
            `;
            
              // 카테고리별로 그룹화
              const violationsByCategory = violations.reduce((acc, v) => {
                if (!acc[v.category]) acc[v.category] = [];
                acc[v.category].push(v);
                return acc;
              }, {});
            
              let categoryIndex = 1;
              for (const [category, items] of Object.entries(violationsByCategory)) {
                validationResult += `#### ${categoryIndex}. ${category}\n`;
                for (const item of items) {
                  validationResult += `- ❌ ${item.message}\n`;
                }
                validationResult += '\n';
                categoryIndex++;
              }
            
              validationResult += `---
            
            ### 📋 위반 상세
            
            `;
            
              let detailIndex = 1;
              for (const violation of violations) {
                validationResult += `${detailIndex}. ${violation.detail}\n`;
                detailIndex++;
              }
            
              validationResult += `
            ---
            *마지막 검증: ${timestamp}*
            <!-- PR_RULE_VALIDATION_END -->`;
            
            } else {
              // 위반 사항이 없는 경우
              validationResult = `<!-- PR_RULE_VALIDATION_START -->
            ## 🤖 PR 규칙 검증 결과
            
            ### ✅ 모든 규칙 준수
            
            이 Pull Request는 정의된 모든 PR 규칙을 준수합니다.
            
            #### 검증 항목
            `;
            
              for (const check of checks) {
                validationResult += `- ✅ ${check}\n`;
              }
            
              if (foundSections.length > 0) {
                validationResult += `- ✅ 필수 섹션: 모두 작성됨\n`;
                for (const section of foundSections) {
                  validationResult += `  - ${section} ✓\n`;
                }
              }
            
              validationResult += `
            ---
            *마지막 검증: ${timestamp}*
            <!-- PR_RULE_VALIDATION_END -->`;
            }
            
            console.log('Validation Result:', validationResult);
            
            // 기존 PR Description에서 검증 결과 제거
            let newBody = pr.body || '';
            const startMarker = '<!-- PR_RULE_VALIDATION_START -->';
            const endMarker = '<!-- PR_RULE_VALIDATION_END -->';
            
            const startIndex = newBody.indexOf(startMarker);
            const endIndex = newBody.indexOf(endMarker);
            
            if (startIndex !== -1 && endIndex !== -1) {
              // 기존 검증 결과 제거
              newBody = newBody.substring(0, startIndex) + 
                        newBody.substring(endIndex + endMarker.length);
            }
            
            // 새 검증 결과를 맨 위에 추가
            newBody = validationResult + '\n\n---\n\n' + newBody.trim();
            
            // PR Description 업데이트
            await github.rest.pulls.update({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: pr.number,
              body: newBody
            });
            
            console.log('PR description updated successfully');
            console.log('Violations found:', violations.length);
            console.log('Non-test lines:', nonTestChangedLines);
            console.log('Test lines:', testChangedLines);
            console.log('Long lines:', longLinesCount);